FIND_LIBRARY(${FORCE_PROJ_NAME}_LIBRARY ${FORCE_PROJ_NAME}
    PATHS @CMAKE_INSTALL_PREFIX@/lib
    NO_DEFAULT_PATH
)
SET(${FORCE_PROJ_NAME}_LIBRARIES ${hirop_force_LIBRARY})

FIND_PATH(${FORCE_PROJ_NAME}_INCLUDE_DIR force/iForce.h
    PATHS @CMAKE_INSTALL_PREFIX@/include/hirop
    NO_DEFAULT_PATH
)

SET(${FORCE_PROJ_NAME}_INCLUDE_DIRS ${hirop_force_INCLUDE_DIR})
